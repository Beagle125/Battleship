const PlayerTypes = Object.freeze({
  HUMAN: "HUMAN",
  COMPUTER: "COMPUTER",
});

const TileTypes = Object.freeze({
  SELECTION: "SELECTION",
  GAMEPLAY: "GAMEPLAY",
});

export { PlayerTypes, TileTypes };
